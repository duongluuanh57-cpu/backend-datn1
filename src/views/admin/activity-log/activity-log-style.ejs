<style>
  .al-item {
    background: var(--color-background-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-element);
    padding: 14px 18px;
    display: flex;
    align-items: flex-start;
    gap: 14px;
    transition: border-color 0.2s ease, box-shadow 0.2s ease;
    cursor: default;
  }
  .al-item:hover { border-color: var(--color-primary); box-shadow: var(--shadow-soft); }
  .al-icon { width: 40px; height: 40px; border-radius: var(--radius-inner); flex-shrink: 0; display: flex; align-items: center; justify-content: center; font-size: 16px; }
  .al-icon.success { background: var(--color-success-light); color: var(--color-success); }
  .al-icon.failure { background: var(--color-error-muted); color: var(--color-error); }
  .al-icon.info { background: var(--color-primary-muted); color: var(--color-primary-dark); }
  .al-body { flex: 1; min-width: 0; }
  .al-header { display: flex; align-items: center; gap: 10px; margin-bottom: 4px; flex-wrap: wrap; }
  .al-user { font-weight: 600; font-size: 14px; color: var(--color-text-primary); }
  .al-action { font-size: 13px; font-weight: 500; padding: 1px 10px; border-radius: var(--radius-full); display: inline-block; }
  .al-action.LOGIN, .al-action.LOGOUT { background: rgba(59,130,246,0.08); color: #3b82f6; }
  .al-action.REGISTER { background: var(--color-success-light); color: var(--color-success); }
  .al-action.PASSWORD_CHANGE { background: var(--color-warning-muted); color: var(--color-warning); }
  .al-action.DELETE_USER, .al-action.DELETE { background: var(--color-error-muted); color: var(--color-error); }
  .al-action.CREATE { background: var(--color-primary-muted); color: var(--color-primary-dark); }
  .al-action.UPDATE, .al-action.UPDATE_USER { background: rgba(139,92,246,0.08); color: #8b5cf6; }
  .al-status { font-size: 11px; font-weight: 600; text-transform: uppercase; letter-spacing: 0.06em; padding: 1px 8px; border-radius: var(--radius-full); }
  .al-status.SUCCESS { background: var(--color-success-light); color: var(--color-success); }
  .al-status.FAILURE { background: var(--color-error-muted); color: var(--color-error); }
  .al-time { font-size: 12px; color: var(--color-text-muted); margin-left: auto; white-space: nowrap; flex-shrink: 0; }
  .al-resource { font-size: 12px; color: var(--color-text-muted); margin-bottom: 4px; }
  .al-resource strong { color: var(--color-text-secondary); font-weight: 500; }
  .al-meta { display: flex; flex-wrap: wrap; gap: 6px; margin-top: 6px; }
  .al-meta-tag { font-size: 11px; padding: 2px 8px; border-radius: var(--radius-inner); background: var(--color-background-body); color: var(--color-text-muted); border: 1px solid var(--color-border); font-family: var(--font-mono); white-space: nowrap; max-width: 100%; overflow: hidden; text-overflow: ellipsis; }
  .al-empty { text-align: center; padding: 60px 20px; color: var(--color-text-muted); font-size: 14px; }
  .al-skeleton { display: flex; align-items: flex-start; gap: 14px; background: var(--color-background-surface); border: 1px solid var(--color-border); border-radius: var(--radius-element); padding: 14px 18px; }
  .al-skeleton-circle { width: 40px; height: 40px; border-radius: var(--radius-inner); flex-shrink: 0; }
</style>
